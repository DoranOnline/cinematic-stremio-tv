{
  "name": "stremio-cinematic-tv",
  "version": "0.1.0",
  "private": true,
  "description": "Android TV and tablet shell for the custom Stremio experience",
  "scripts": {
    "sync": "npx cap sync android",
    "build:debug": "npm run sync && cd android && gradlew.bat assembleDebug",
    "build:release": "npm run sync && cd android && gradlew.bat assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "8.5.0",
    "@capacitor/core": "8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.0"
  }
}
